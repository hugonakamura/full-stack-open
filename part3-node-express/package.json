{
  "name": "part3-node-express",
  "version": "1.0.0",
  "description": "node and express full stack open class",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../introdemo && npm run build && shx cp -r dist ../part3-node-express",
    "deploy:full": "npm run build:ui && cd ../ && git add . && git commit -m uibuild && git push"
  },
  "author": "hugo george nakamura",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}